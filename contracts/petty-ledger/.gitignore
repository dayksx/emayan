# Build outputs (Cargo target-dir is vault/target — see .cargo/config.toml)
vault/target/
escrow/target/
target/
*.wasm

# Wallets (keep private!)
.wallets/

# Bedrock internal
.bedrock/

# OS files
.DS_Store
